{
  "name": "track-finance-backend",
  "version": "1.0.0",
  "description": "Backend for TrackFinance app",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --no-cors --static ./nonexistent"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "engines": {
    "node": "18.x"
  }
}